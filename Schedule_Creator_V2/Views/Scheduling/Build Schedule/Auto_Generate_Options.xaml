<Window x:Class="Schedule_Creator_V2.Auto_Generate_Options"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:enums="clr-namespace:Schedule_Creator_V2.Models.Enums"
        Title="Auto Generate Schedule"
        Width="760"
        Height="800"
        MinWidth="700"
        MinHeight="650"
        MaxHeight="950"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Background="#F1F5F3"
        FontFamily="Segoe UI">

    <Window.Resources>

        <!-- Card -->
        <Style x:Key="CardStyle"
               TargetType="Border">

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderBrush"
                    Value="#DDE5E1"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="CornerRadius"
                    Value="12"/>

            <Setter Property="Padding"
                    Value="20"/>

            <Setter Property="Effect">

                <Setter.Value>

                    <DropShadowEffect BlurRadius="14"
                                      Direction="270"
                                      ShadowDepth="3"
                                      Opacity="0.08"
                                      Color="#143D30"/>

                </Setter.Value>

            </Setter>

        </Style>

        <!-- Time picker -->
        <Style TargetType="{x:Type xctk:TimePicker}">

            <Setter Property="Height"
                    Value="34"/>

            <Setter Property="MinWidth"
                    Value="105"/>

            <Setter Property="Padding"
                    Value="7,3"/>

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="Foreground"
                    Value="#1F2937"/>

            <Setter Property="BorderBrush"
                    Value="#BFCBC5"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="FontSize"
                    Value="12"/>

            <Setter Property="Format"
                    Value="Custom"/>

            <Setter Property="FormatString"
                    Value="h:mm tt"/>

            <Setter Property="TimeInterval"
                    Value="00:15:00"/>

        </Style>

        <!-- ComboBox -->
        <Style TargetType="ComboBox">

            <Setter Property="Height"
                    Value="34"/>

            <Setter Property="Padding"
                    Value="8,3"/>

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="Foreground"
                    Value="#1F2937"/>

            <Setter Property="BorderBrush"
                    Value="#BFCBC5"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>

        </Style>

        <!-- Checkbox -->
        <Style TargetType="CheckBox">

            <Setter Property="Foreground"
                    Value="#293A34"/>

            <Setter Property="FontSize"
                    Value="14"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>

        </Style>

        <!-- Primary button -->
        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button">

            <Setter Property="MinWidth"
                    Value="130"/>

            <Setter Property="Height"
                    Value="42"/>

            <Setter Property="Padding"
                    Value="18,7"/>

            <Setter Property="Background"
                    Value="#0F5640"/>

            <Setter Property="BorderBrush"
                    Value="#0F5640"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Foreground"
                    Value="White"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>

        <!-- Secondary button -->
        <Style x:Key="SecondaryButtonStyle"
               TargetType="Button">

            <Setter Property="MinWidth"
                    Value="105"/>

            <Setter Property="Height"
                    Value="42"/>

            <Setter Property="Padding"
                    Value="18,7"/>

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderBrush"
                    Value="#BFCBC5"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Foreground"
                    Value="#374151"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>

        <!-- Small button -->
        <Style x:Key="SmallButtonStyle"
               TargetType="Button">

            <Setter Property="Height"
                    Value="32"/>

            <Setter Property="Padding"
                    Value="12,4"/>

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderBrush"
                    Value="#BFCBC5"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Foreground"
                    Value="#374151"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>

    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Padding="24,22"
                Background="#0F5640">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="52"
                        Height="52"
                        Background="#25FFFFFF"
                        CornerRadius="12">

                    <TextBlock HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="25"
                               Foreground="White"
                               Text="&#xE895;"/>

                </Border>

                <StackPanel Grid.Column="2"
                            VerticalAlignment="Center">

                    <TextBlock Text="Auto Generate Schedule"
                               FontSize="23"
                               FontWeight="SemiBold"
                               Foreground="White"/>

                    <TextBlock Margin="0,4,0,0"
                               Text="Define the shifts AutoGen should fill and optionally exclude staff."
                               FontSize="13"
                               Foreground="#D5E9E2"/>

                </StackPanel>

            </Grid>

        </Border>

        <!-- Main content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">

            <StackPanel Margin="24">

                <!-- Shift templates -->
                <Border Style="{StaticResource CardStyle}">

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="16"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Heading -->
                        <Grid Grid.Row="0">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>

                                <TextBlock Text="Shifts to Follow"
                                           FontSize="17"
                                           FontWeight="SemiBold"
                                           Foreground="#1F2937"/>

                                <TextBlock Margin="0,5,15,0"
                                           Text="Each row represents one shift AutoGen will attempt to fill on every configured operating day."
                                           FontSize="13"
                                           LineHeight="19"
                                           TextWrapping="Wrap"
                                           Foreground="#6B7280"/>

                            </StackPanel>

                            <Button Grid.Column="1"
                                    Style="{StaticResource SmallButtonStyle}"
                                    Click="AddShift_Click"
                                    Content="+ Add Shift"/>

                        </Grid>

                        <!-- Shift grid -->
                        <DataGrid x:Name="ShiftGrid"
                                  Grid.Row="2"
                                  Height="270"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserResizeRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#E1E8E4"
                                  BorderBrush="#DDE5E1"
                                  BorderThickness="1"
                                  Background="White"
                                  RowHeight="52">

                            <DataGrid.Columns>

                                <!-- Start -->
                                <DataGridTemplateColumn Header="Start"
                                                        Width="*">

                                    <DataGridTemplateColumn.CellTemplate>

                                        <DataTemplate>

                                            <xctk:TimePicker Margin="6"
                                                             Value="{Binding StartTime,
                                                                             Mode=TwoWay,
                                                                             UpdateSourceTrigger=PropertyChanged}"/>

                                        </DataTemplate>

                                    </DataGridTemplateColumn.CellTemplate>

                                </DataGridTemplateColumn>

                                <!-- End -->
                                <DataGridTemplateColumn Header="End"
                                                        Width="*">

                                    <DataGridTemplateColumn.CellTemplate>

                                        <DataTemplate>

                                            <xctk:TimePicker Margin="6"
                                                             Value="{Binding EndTime,
                                                                             Mode=TwoWay,
                                                                             UpdateSourceTrigger=PropertyChanged}"/>

                                        </DataTemplate>

                                    </DataGridTemplateColumn.CellTemplate>

                                </DataGridTemplateColumn>

                                <!-- Type -->
                                <DataGridTemplateColumn Header="Shift Type"
                                                        Width="170">

                                    <DataGridTemplateColumn.CellTemplate>

                                        <DataTemplate>

                                            <ComboBox Margin="6"
                                                      SelectedValue="{Binding ShiftType,
                                                                              Mode=TwoWay,
                                                                              UpdateSourceTrigger=PropertyChanged}"
                                                      SelectedValuePath="Tag">

                                                <ComboBoxItem Content="Any Staff"
                                                              Tag="{x:Static enums:AutoGenShiftType.AnyStaff}"/>

                                                <ComboBoxItem Content="Leadership"
                                                              Tag="{x:Static enums:AutoGenShiftType.Leadership}"/>

                                            </ComboBox>

                                        </DataTemplate>

                                    </DataGridTemplateColumn.CellTemplate>

                                </DataGridTemplateColumn>

                                <!-- Delete -->
                                <DataGridTemplateColumn Header=""
                                                        Width="90">

                                    <DataGridTemplateColumn.CellTemplate>

                                        <DataTemplate>

                                            <Button Margin="6"
                                                    Height="32"
                                                    Padding="10,3"
                                                    Background="#FFF1EF"
                                                    BorderBrush="#EDC0BA"
                                                    Foreground="#B42318"
                                                    Content="Delete"
                                                    Click="DeleteShift_Click"/>

                                        </DataTemplate>

                                    </DataGridTemplateColumn.CellTemplate>

                                </DataGridTemplateColumn>

                            </DataGrid.Columns>

                        </DataGrid>

                    </Grid>

                </Border>

                <!-- Type explanation -->
                <Border Margin="0,14,0,0"
                        Padding="14"
                        Background="#EEF5FA"
                        BorderBrush="#C9DBE8"
                        BorderThickness="1"
                        CornerRadius="8">

                    <StackPanel>

                        <TextBlock Text="Shift Types"
                                   FontWeight="SemiBold"
                                   Foreground="#365B70"/>

                        <TextBlock Margin="0,5,0,0"
                                   Text="Leadership: only leadership-qualified staff may fill the shift.  Any Staff: any eligible staff member may fill the shift, including leadership."
                                   FontSize="12"
                                   LineHeight="18"
                                   TextWrapping="Wrap"
                                   Foreground="#536D7D"/>

                    </StackPanel>

                </Border>

                <!-- Exclusions -->
                <Border Margin="0,18,0,0"
                        Style="{StaticResource CardStyle}">

                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="14"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0">

                            <TextBlock Text="Exclude Staff"
                                       FontSize="17"
                                       FontWeight="SemiBold"
                                       Foreground="#1F2937"/>

                            <TextBlock Margin="0,5,0,0"
                                       Text="Selected staff members will not be considered by AutoGen."
                                       FontSize="13"
                                       Foreground="#6B7280"/>

                        </StackPanel>

                        <Border Grid.Row="2"
                                MinHeight="180"
                                MaxHeight="260"
                                Background="#FAFCFB"
                                BorderBrush="#DDE5E1"
                                BorderThickness="1"
                                CornerRadius="8"
                                ClipToBounds="True">

                            <ScrollViewer Padding="10"
                                          VerticalScrollBarVisibility="Auto">

                                <ItemsControl x:Name="StaffCheckboxList">

                                    <ItemsControl.ItemTemplate>

                                        <DataTemplate>

                                            <Border Margin="0,0,0,7"
                                                    Padding="12,10"
                                                    Background="White"
                                                    BorderBrush="#E1E8E4"
                                                    BorderThickness="1"
                                                    CornerRadius="7">

                                                <CheckBox Content="{Binding Name}"
                                                          IsChecked="{Binding IsExcluded}"/>

                                            </Border>

                                        </DataTemplate>

                                    </ItemsControl.ItemTemplate>

                                </ItemsControl>

                            </ScrollViewer>

                        </Border>

                    </Grid>

                </Border>

            </StackPanel>

        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2"
                Padding="20,16"
                Background="White"
                BorderBrush="#DDE5E1"
                BorderThickness="0,1,0,0">

            <StackPanel HorizontalAlignment="Right"
                        Orientation="Horizontal">

                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Content="Cancel"
                        IsCancel="True"/>

                <Button Margin="10,0,0,0"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="Generate_Click"
                        IsDefault="True"
                        Content="Generate Schedule"/>

            </StackPanel>

        </Border>

    </Grid>

</Window>