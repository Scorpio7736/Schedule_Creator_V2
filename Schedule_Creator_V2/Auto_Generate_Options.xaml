<Window
    x:Class="Schedule_Creator_V2.Auto_Generate_Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Auto Generate Options"
    Width="420"
    Height="520"
    MinWidth="380"
    MinHeight="420"
    ResizeMode="CanMinimize"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            FontSize="16"
            FontWeight="Bold"
            Text="Auto Generate Schedule" />

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="How many staff do you want on each day?" />
            <TextBox
                x:Name="StaffPerDayBox"
                Width="80"
                Margin="0,6,0,0"
                HorizontalAlignment="Left"
                VerticalContentAlignment="Center"
                Text="3" />
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Exclude Staff" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="StaffCheckboxList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox
                                Margin="0,2"
                                Content="{Binding Name}"
                                IsChecked="{Binding IsExcluded}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <StackPanel
            Grid.Row="3"
            Margin="0,16,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="80"
                Margin="0,0,8,0"
                Content="Cancel"
                IsCancel="True" />
            <Button
                Width="80"
                Content="Generate"
                Click="Generate_Click"
                IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
