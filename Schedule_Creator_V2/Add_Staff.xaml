<Page
    x:Class="Schedule_Creator_V2.Add_Staff"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Schedule_Creator_V2"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Add_Staff"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Border
        Margin="20"
        Padding="20"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Background="White"
        BorderBrush="#DDD"
        BorderThickness="1"
        CornerRadius="10">


            <Grid Margin="20" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  First Name  -->
                <Label
            x:Name="FirstNameLabel"
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Center"
            Content="First Name:" />
                <TextBox
            x:Name="FirstNameBox"
            Grid.Row="0"
            Grid.Column="1"
            Margin="0,5" />

                <!--  Middle Name  -->
                <Label
            x:Name="MiddleNameLabel"
            Grid.Row="1"
            Grid.Column="0"
            VerticalAlignment="Center"
            Content="Middle Name:" />
                <TextBox
            x:Name="MiddleNameBox"
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,5" />

                <!--  Last Name  -->
                <Label
            x:Name="LastNameLabel"
            Grid.Row="2"
            Grid.Column="0"
            VerticalAlignment="Center"
            Content="Last Name:" />
                <TextBox
            x:Name="LastNameBox"
            Grid.Row="2"
            Grid.Column="1"
            Margin="0,5" />

                <!--  Job Position  -->
                <Label
            x:Name="PositionLabel"
            Grid.Row="3"
            Grid.Column="0"
            VerticalAlignment="Center"
            Content="Job Position:" />
                <ComboBox
            x:Name="JobPositionComboBox"
            Grid.Row="3"
            Grid.Column="1"
            Margin="0,5" />

                <!--  Email  -->
                <Label
            x:Name="StudentEmailLabel"
            Grid.Row="4"
            Grid.Column="0"
            VerticalAlignment="Center"
            Content="Student Email:" />
                <TextBox
            x:Name="StudentEmailBox"
            Grid.Row="4"
            Grid.Column="1"
            Margin="0,5" />

                <!--  Buttons  -->
                <StackPanel
            Grid.Row="5"
            Grid.ColumnSpan="2"
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
                    <Button
                Width="150"
                Height="50"
                Margin="10,0"
                Click="AddEmployee_Click"
                Content="Save" />
                    <Button
                Width="150"
                Height="50"
                Margin="10,0"
                Click="Cancel_Click"
                Content="Cancel" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
